/*
 * JS for startScreen generated by Appery.io
 */

Apperyio.getProjectGUID = function() {
    return 'c14f8a8c-776f-4e7f-8d3d-bc94985d8c58';
};

function navigateTo(outcome, useAjax) {
    Apperyio.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Apperyio.adjustContentHeightWithPadding();
}

function adjustContentHeightWithPadding(_page) {
    Apperyio.adjustContentHeightWithPadding(_page);
}

function setDetailContent(pageUrl) {
    Apperyio.setDetailContent(pageUrl);
}

Apperyio.AppPages = [{
    "name": "confirm_driver",
    "location": "confirm_driver.html"
}, {
    "name": "confirm_customer",
    "location": "confirm_customer.html"
}, {
    "name": "confirm_truck",
    "location": "confirm_truck.html"
}, {
    "name": "startScreen",
    "location": "startScreen.html"
}, {
    "name": "monitor",
    "location": "monitor.html"
}];

function startScreen_js() {

    /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'mobileimage_2': 'startScreen_mobileimage_2',
        'start_shift_button': 'startScreen_start_shift_button',
        'verion': 'startScreen_verion'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    /*
     * Nonvisual components
     */

    Apperyio.mappings = Apperyio.mappings || {};

    Apperyio.datasources = Apperyio.datasources || {};

    Apperyio.CurrentScreen = 'startScreen';
    _.chain(Apperyio.mappings).filter(function(m) {
        return m.homeScreen === Apperyio.CurrentScreen;
    }).each(Apperyio.UIHandler.hideTemplateComponents);

    /*
     * Events and handlers
     */

    // On Load
    var startScreen_onLoad = function() {
            startScreen_elementsExtraJS();

            try {
                $a.storage["loadStatus"].update("$", "unloaded")
            } catch (e) {
                console.error(e)
            };
            //localStorage.setItem('currentSiteId', -1);

            //Apperyio("gps_test").val(localStorage.getItem("lat")+","+localStorage.getItem("lon"));
            //Apperyio("gps_test").val(localStorage.getItem("speed"));
            try {
                $a.storage["visitActive"].update("$", "false")
            } catch (e) {
                console.error(e)
            };

            startScreen_deviceEvents();
            startScreen_windowEvents();
            startScreen_elementsEvents();
        };

    // screen window events


    function startScreen_windowEvents() {

        $('#startScreen').bind('pageshow orientationchange', function() {
            var _page = this;
            adjustContentHeightWithPadding(_page);
        });
        $('#startScreen').on({
            pageshow: function(event) {
                window.localStorage["currentSiteId"] = "-1";
                window.localStorage["activeVisitId"] = "none";
                console.log("StartScreen Current Site Id:", localStorage.getItem("currentSiteId"));
                console.log("StartScreen ActiveVisitId:", localStorage.getItem("activeVisitId"));
            },
        });

    };

    // device events


    function startScreen_deviceEvents() {
        document.addEventListener("deviceready", function() {
            window.localStorage["currentSiteId"] = "-1";
            window.localStorage["activeVisitId"] = "none";
            var plot = cordova.require("com.plotprojects.plugin.PlotCordovaPlugin");
            var config = plot.exampleConfiguration;
            config.publicKey = "u9xZRQdFsdDRgnMu"; //put your public key here
            plot.init(config);;

        });
    };

    // screen elements extra js


    function startScreen_elementsExtraJS() {
        // screen (startScreen) extra code

    };

    // screen elements handler


    function startScreen_elementsEvents() {
        $(document).on("click", "a :input,a a,a fieldset label", function(event) {
            event.stopPropagation();
        });

        $(document).off("click", '#startScreen_mobilecontainer [name="start_shift_button"]').on({
            click: function(event) {
                if (!$(this).attr('disabled')) {
                    Apperyio.navigateTo('confirm_driver', {
                        reverse: false
                    });
                    salesforceLogin();

                }
            },
        }, '#startScreen_mobilecontainer [name="start_shift_button"]');

    };

    $(document).off("pagebeforeshow", "#startScreen").on("pagebeforeshow", "#startScreen", function(event, ui) {
        Apperyio.CurrentScreen = "startScreen";
        _.chain(Apperyio.mappings).filter(function(m) {
            return m.homeScreen === Apperyio.CurrentScreen;
        }).each(Apperyio.UIHandler.hideTemplateComponents);
    });

    startScreen_onLoad();
};

$(document).off("pagecreate", "#startScreen").on("pagecreate", "#startScreen", function(event, ui) {
    Apperyio.processSelectMenu($(this));
    startScreen_js();
});